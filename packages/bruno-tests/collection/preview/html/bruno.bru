meta {
  name: bruno
  type: http
  seq: 1
}

get {
  url: {{host}}/html
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("should return parsed html", function() {
    const headers = res.getHeaders();
    expect(headers['content-type']).to.eql("text/html; charset=utf-8");
  });
  
}
