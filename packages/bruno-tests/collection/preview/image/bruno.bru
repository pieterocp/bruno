meta {
  name: bruno
  type: http
  seq: 1
}

get {
  url: {{host}}/favicon
  body: none
  auth: none
}

tests {
  test("should return parsed image", function() {
    const headers = res.getHeaders();
    expect(headers['content-type']).to.eql("image/x-icon");
  });
  
}
