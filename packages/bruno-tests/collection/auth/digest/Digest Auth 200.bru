meta {
  name: Digest Auth 200
  type: http
  seq: 1
}

get {
  url: {{auth-host}}/api/auth/digest-auth/auth
  body: none
  auth: digest
}

auth:digest {
  username: foo
  password: passwd
}

assert {
  res.status: eq 200
  res.body.authenticated: isTruthy
}
