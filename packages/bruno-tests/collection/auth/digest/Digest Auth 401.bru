meta {
  name: Digest Auth 401
  type: http
  seq: 2
}

get {
  url: {{auth-host}}/api/auth/digest-auth/auth
  body: none
  auth: digest
}

auth:digest {
  username: foo
  password: passwd
}

assert {
  res.status: eq 401
}
